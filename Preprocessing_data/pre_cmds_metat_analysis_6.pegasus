#!/bin/bash
END=792
for i in $(seq 1 $END);
do
	echo "TASK pre_metaT_${i}_${1}_5 -c 1 bash -c 'Rscript pre_MetaT_analysis_6.R $1 $i 2> pre_metat_${1}_${i}_6.e'" >> pre_comds_metat_${1}_6.pegasus
done

